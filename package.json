{
  "name": "local-host-service",
  "version": "0.1.0",
  "description": "Local Host Service — host HTML and generate websites from prompts (with optional OpenAI integration)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "multer": "^1.4.4"
  }
}
